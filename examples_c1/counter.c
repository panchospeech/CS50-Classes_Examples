#include <stdio.h>
#include <cs50.h>

int main (void);
{
    // creating variable
    int counter = 0;

    // for increment by one
    counter = counter + 1;
    counter += 1; // you can change the 1 to any other number
    counter++;  // only for one counter--; is substraction

    //counter = counter + 1; or counter += 1; (sintactic sugar) or also counter++; (ss too, only for adding or substracting 1)
    // this doc is only for reference to sintactic sugar


}